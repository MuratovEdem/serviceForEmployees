spring:
  application:
    name: serviceForEmployees
  datasource:
    url: jdbc:postgresql://localhost:5432/serviceForEmployees
    username: postgres
    password: kingring1
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop